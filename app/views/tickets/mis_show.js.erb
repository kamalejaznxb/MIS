jQuery('.cancel').trigger('click');

jQuery('.inner').after("<div id='popup'><%= escape_javascript(render :partial => "tickets/MIS/show") %> </div>")

set_position();

jQuery(".stars-wrapper").rating();

