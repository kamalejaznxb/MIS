<td><%= ticket.ticket_category %></td>
<td><%= ticket.ticket_type %></td>
<td><%= ticket.ticket_status %></td>
<td><%= ticket.user %></td>
<td><%= ticket.assigned %></td>
<td><%= ticket.refered %></td>
<td><%= ticket.age %></td>
<td><%= ticket.created_at %></td>
<td>
  <%= link_to 'Show', ticket_path( ticket ), :remote => true %>
  |
  <%= link_to "Add Comment", new_ticket_comment_path(ticket, :ticket => true), :remote => true %>
  |
  <%= link_to "Assign to", edit_ticket_path(ticket, :assign_to => true), :remote => true %>
  |
  <%= link_to "Refered To", edit_ticket_path(ticket, :refered_to => true), :remote => true %>
</td>









