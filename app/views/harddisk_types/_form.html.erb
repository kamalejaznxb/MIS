<%= form_for(@harddisk_type) do |f| %>
  <% if @harddisk_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@harddisk_type.errors.count, "error") %> prohibited this harddisk_type from being saved:</h2>

      <ul>
      <% @harddisk_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :size %><br />
    <%= f.text_field :size %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br />
    <%= f.text_field :capacity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
